<Window x:Class="lockin.wpf.Views.QuestionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HOSTAGE SITUATION" Height="300" Width="450" 
        WindowStartupLocation="CenterScreen" Topmost="True" ResizeMode="NoResize"
        Background="#0F0F0F">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Question Text -->
        <TextBlock x:Name="QuestionTextBlock" TextWrapping="Wrap" FontSize="18" 
                   FontWeight="Bold" TextAlignment="Center" VerticalAlignment="Center" 
                   Foreground="White"/>

        <!-- 4 Answer Buttons (Blue) -->
        <UniformGrid Grid.Row="1" Columns="2" Rows="2" Margin="0,20,0,0">
            <Button x:Name="BtnA" Margin="5" Padding="15" Click="Answer_Click" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            <Button x:Name="BtnB" Margin="5" Padding="15" Click="Answer_Click" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            <Button x:Name="BtnC" Margin="5" Padding="15" Click="Answer_Click" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            <Button x:Name="BtnD" Margin="5" Padding="15" Click="Answer_Click" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
        </UniformGrid>
    </Grid>
</Window>